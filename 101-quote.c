#include <stdio.h>

/**
*quote - prints a quote
*
*Return:always 0 (success)
*/
int main()
{
char doublequotes = '"';
char space = ' ';
char dash = '-';
char comma = ','
printf("and that piece of art is useful%s%s%sDora Korpar%s%s2015%s10%s19\n", doublequotes, space, dash, comma, space, dash, dash);
return(0)
}
